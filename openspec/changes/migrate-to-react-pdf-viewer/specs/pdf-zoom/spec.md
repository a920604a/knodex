## 新增需求

### 需求：縮放控制
系統必須提供放大、縮小、重設縮放的控制項於閱讀器 Toolbar。

#### 情境：點擊放大
- **當** 使用者點擊放大按鈕
- **則** PDF 頁面顯示比例必須增大

#### 情境：點擊縮小
- **當** 使用者點擊縮小按鈕
- **則** PDF 頁面顯示比例必須減小

#### 情境：顯示目前縮放比例
- **當** 閱讀器載入或縮放改變
- **則** Toolbar 必須顯示目前縮放比例數值（如 100%）
